<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirect Trailing Slashes If Not A Folder
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Istniejace pliki / katalogi
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # --- XAMPP: Alias /Przetargi -> backend/public ---
    RewriteCond %{REQUEST_URI} ^/Przetargi/api
    RewriteRule ^api(/.*)?$ /Przetargi/index.php [L]

    RewriteCond %{REQUEST_URI} ^/Przetargi/
    RewriteRule ^ /Przetargi/index.html [L]

    # --- Produkcja: DocumentRoot = backend/public ---
    RewriteCond %{REQUEST_URI} !^/Przetargi/
    RewriteRule ^api(/.*)?$ index.php [L]

    RewriteCond %{REQUEST_URI} !^/Przetargi/
    RewriteRule ^ index.html [L]
</IfModule>

DirectoryIndex index.html index.php
